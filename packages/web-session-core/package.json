{
  "name": "@niwar-devforge/web-session-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-safe session revalidation lifecycle for Niwar DevForge web products.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "MODULE.md",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
